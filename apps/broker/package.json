{
  "name": "@cluefin/broker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm --prefix ../.. run build --workspaces --if-present && node --env-file=../../.env dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0",
    "@cluefin/cloudflare": "0.0.1",
    "@cluefin/securities": "0.0.1"
  },
  "devDependencies": {
    "@types/node": "^25.1.0"
  }
}
