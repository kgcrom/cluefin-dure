{
  "name": "@cluefin/dure",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/index.js",
    "test": "npm run build && vitest run"
  },
  "devDependencies": {
    "@types/node": "^25.1.0"
  }
}
